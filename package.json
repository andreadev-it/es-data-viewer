{
  "name": "galaxy-viewer",
  "version": "1.0.0",
  "description": "A typescript and canvas based viewer for endless sky star systems",
  "scripts": {
    "start": "parcel src/index.html --dist-dir dist",
    "build": "parcel build src/index.html --dist-dir dist --public-url ./",
    "test": "echo \"Error: no test specified\" && exit 1"
  },
  "author": "Andrea Dragotta",
  "license": "ISC",
  "devDependencies": {
    "parcel": "^2.8.3"
  },
  "dependencies": {
    "@andreadev/canvas-lib": "^1.0.0",
    "es-data-parser": "^1.0.0"
  }
}
